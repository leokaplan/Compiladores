/* Teste de hierarquia */

void main () {
	int a, b, c;

	a = 3;
	b = 5;
	c = -8;

	a = -b + 3;
	b = -a - 3;
	c = -a || -c * -2;
	c = c || a + 3;
	b = c || a && c - 1; 
	a = (c || a && c - 1 || !(a > b)) * b +  -a ;
	
	return;
}
